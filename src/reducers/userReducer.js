let initialState = {
  username: '',
}

let userReducer = (state = initialState, action) => {
  switch (action.type) {
    case "value":
      
      break;
  
    default:
      return state;
  }
}

export default userReducer;